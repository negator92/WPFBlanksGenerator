<Window x:Class="WPFBlanksGenerator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        DataContext="{Binding ApplicationViewModel, Source={x:Static Application.Current}}" 
        Title="WPF template creator" MinHeight="300" MinWidth="450" Background="#333333">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/WPFBlanksGenerator;component/StylesResources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <DockPanel Margin="0">
        <DockPanel DockPanel.Dock="Top">
            <TextBlock Text="Name:" DockPanel.Dock="Left"/>
            <TextBox Text="{Binding Solution.Name}" />
        </DockPanel>
        <Button DockPanel.Dock="Bottom" Margin="5,0,5,5">
            <TextBlock Text="Create WPF project" Margin="5,0,5,0"/>
        </Button>
        <DockPanel DockPanel.Dock="Bottom">
            <Button>
                <TextBlock Text="Choose" Margin="5,0,5,0"/>
            </Button>
            <TextBlock Text="Path:" DockPanel.Dock="Left" />
            <TextBox Text="{Binding Solution.Path}" />
        </DockPanel>
        <ScrollViewer Focusable="False" VerticalScrollBarVisibility="Visible" Margin="5,0,5,0">
            <ItemsControl Focusable="False" VerticalAlignment="Center"
                          ItemsSource="{Binding Solution.ProjectsList}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="1" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <DockPanel>
                            <TextBlock Text="Project:" DockPanel.Dock="Left" />
                            <ComboBox DockPanel.Dock="Right" />
                            <Button DockPanel.Dock="Right">
                                <TextBlock Text="+" Margin="5,0,5,0" />
                            </Button>
                            <TextBox Text="{Binding Name}" />
                        </DockPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </DockPanel>
</Window>
